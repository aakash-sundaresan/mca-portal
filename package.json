{
  "name": "mca-portal",
  "version": "1.0.0",
  "description": "Simple vanilla JavaScript S3 document reader",
  "main": "index.html",
  "scripts": {
    "start": "python -m http.server 8000",
    "serve": "npx serve ."
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.922.0"
  }
}
